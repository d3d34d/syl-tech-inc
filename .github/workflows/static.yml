# DISABLED — This workflow deployed raw source (no build step) and caused blank pages.
# The correct pipeline is deploy.yml which builds with Vite first.
name: Deploy static content to Pages (DISABLED)

on:
  workflow_dispatch: # manual only — never auto-runs

